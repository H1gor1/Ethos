info:
  name: Create User
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/auth/register
  body:
    type: json
    data: |-
      {
        "username": "Higor Lino2",
        "email": "contato2@higorlino.dev",
        "password": "16122002"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
