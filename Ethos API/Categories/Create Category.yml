info:
  name: Create Category
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/category
  body:
    type: json
    data: |-
      {
        "name": "Categoria 1",
        "color": "#0000FF"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
