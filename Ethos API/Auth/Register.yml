info:
  name: Register
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/auth/register
  body:
    type: json
    data: |-
      {
        "username": "higorlino2",
        "email": "suporte@higorlino.dev",
        "password": "16122002"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
